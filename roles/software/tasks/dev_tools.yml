- name: Install Development Tools
  package:
    name:
      - rustup
      - python3
      - nodejs
      - nvm
    state: present

- name: Install npm global packages
  npm:
    name: '{{ item }}'
    global: yes
  loop:
    - pnpm
    - degit
    - nodemon
